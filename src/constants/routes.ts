export enum Routes {
  HOME = '/',
  MAP = '/map',
  NEWS = '/news',
  NEWS_ITEM = '/news/:slug',
  REQUESTS = '/requests',
  PROFILE = '/profile',
  LOGIN = '/login',
  REGISTER = '/register',
  RESET = '/password/reset',
}
