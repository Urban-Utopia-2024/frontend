export enum Routes {
  HOME = '/',
  MAP = '/map',
  NEWS = '/news',
  REQUESTS = '/requests',
  PROFILE = '/profile',
  LOGIN = '/login',
  REGISTER = '/register',
  RESET = '/password/reset',
  APPROVE = '/register/code',
}
